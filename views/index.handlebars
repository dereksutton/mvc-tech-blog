<div class="container">
    {{#each posts}}
        <div class="post">
            <h2><a href="/post/{{id}}">{{title}}</a></h2>
            <p>{{body}}</p>
            <p>Posted by: {{User.username}}</p>
            <p>On: {{createdAt}}</p>
        </div>
    {{/each}}
</div>